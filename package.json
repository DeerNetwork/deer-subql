{
  "name": "deer-subql",
  "version": "0.1.0",
  "description": "subql for deer network",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "prepack": "rm -rf dist && npm build",
    "test": "jest",
    "codegen": "./node_modules/.bin/subql codegen"
  },
  "homepage": "https://github.com/subquery/subql-starter",
  "repository": "github:subquery/subql-starter",
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "license": "MIT",
  "devDependencies": {
    "@deernetwork/type-definitions": "^0.4.6",
    "@polkadot/api": "^6",
    "@subql/cli": "latest",
    "@subql/node": "latest",
    "@subql/query": "latest",
    "@subql/types": "latest",
    "typescript": "^4.1.3"
  }
}
